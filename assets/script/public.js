document.addEventListener("DOMContentLoaded", function () {
    // Cards already animate in via CSS on page load (see fadeInUp).
    // This is a hook for future JS-driven interactivity (e.g. live status refresh).
});